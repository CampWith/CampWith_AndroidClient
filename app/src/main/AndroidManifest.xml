<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.campwith">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".GlobalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name="com.example.campwith.presentation.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.campwith.presentation.login.view.LoginActivity" />
        <activity android:name=".presentation.reviewwrite.view.ReviewWriteActivity" />
        <activity android:name=".presentation.camptool.view.CampToolActivity" />
        <activity android:name=".presentation.campdetail.view.CampDetailActivity" />
        <activity android:name=".presentation.main.view.MainActivity" />
    </application>

</manifest>